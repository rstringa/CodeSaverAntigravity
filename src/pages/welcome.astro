---
import Layout from "../layouts/Layout.astro";
import {
    Code2,
    Zap,
    Shield,
    Layout as LayoutIcon,
    LogIn,
    UserPlus,
} from "@lucide/astro";
---

<Layout title="CodeSaverAntigravity | Tu librería de código personal">
    <div class="landing-page">
        <!-- Hero Section -->
        <section class="hero">
            <div class="hero-content">
                <div class="badge">Nuevo: Soporte Multi-usuario</div>
                <h1>
                    Guarda tu código con <span>estilo antiprovisional</span>
                </h1>
                <p>
                    La herramienta definitiva para desarrolladores que quieren
                    organizar sus snippets, optimizar su flujo de trabajo y no
                    volver a buscar el mismo código en Google por décima vez.
                </p>
                <div class="hero-cta">
                    <a href="/register" class="btn-primary">
                        <UserPlus size={20} />
                        Empieza Gratis
                    </a>
                    <a href="/login" class="btn-secondary">
                        <LogIn size={20} />
                        Iniciar Sesión
                    </a>
                </div>
            </div>
            <div class="hero-visual">
                <div class="code-card-floating">
                    <div class="card-header">
                        <div class="dots">
                            <span></span><span></span><span></span>
                        </div>
                    </div>
                    <pre><code>{`function antigravity() {
  const code = "organizado";
  return code.save();
}`}</code></pre>
                </div>
            </div>
        </section>

        <!-- Features Section -->
        <section class="features">
            <div class="feature-card">
                <div class="feature-icon"><Zap /></div>
                <h3>Acceso Rápido</h3>
                <p>
                    Busca y copia tus snippets en milisegundos. La velocidad es
                    nuestra prioridad.
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon"><Shield /></div>
                <h3>Seguro y Privado</h3>
                <p>
                    Tus datos están protegidos y solo tú puedes acceder a tu
                    librería personal.
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon"><LayoutIcon /></div>
                <h3>Organización Total</h3>
                <p>
                    Categorías, subcategorías y etiquetas para que nunca pierdas
                    nada.
                </p>
            </div>
        </section>
    </div>
</Layout>

<style>
    .landing-page {
        padding-top: 4rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .hero {
        display: grid;
        grid-template-columns: 1.2fr 1fr;
        gap: 4rem;
        align-items: center;
        padding: 4rem 2rem;
        margin-bottom: 6rem;
    }

    .hero-content h1 {
        font-size: 4rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: 1.5rem;
        letter-spacing: -0.04em;
    }

    .hero-content h1 span {
        color: var(--accent);
        background: linear-gradient(120deg, var(--accent) 0%, #afffb0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-content p {
        font-size: 1.25rem;
        color: var(--text-secondary);
        margin-bottom: 2.5rem;
        line-height: 1.6;
    }

    .badge {
        display: inline-block;
        padding: 0.5rem 1rem;
        background: rgba(var(--accent-rgb), 0.1);
        color: var(--accent);
        border-radius: 100px;
        font-size: 0.875rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        border: 1px solid rgba(var(--accent-rgb), 0.2);
    }

    .hero-cta {
        display: flex;
        gap: 1.5rem;
    }

    .btn-primary {
        background: var(--accent);
        color: #000;
        padding: 1rem 2rem;
        border-radius: var(--radius-md);
        font-weight: 700;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        transition: all 0.2s;
    }

    .btn-primary:hover {
        background: var(--accent-hover);
        transform: translateY(-2px);
    }

    .btn-secondary {
        background: var(--bg-tertiary);
        color: var(--text-primary);
        padding: 1rem 2rem;
        border-radius: var(--radius-md);
        font-weight: 700;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        border: 1px solid var(--border-primary);
        transition: all 0.2s;
    }

    .btn-secondary:hover {
        background: var(--bg-primary);
        border-color: var(--text-secondary);
    }

    .hero-visual {
        position: relative;
    }

    .code-card-floating {
        background: #0d0d0d;
        border: 1px solid var(--border-primary);
        border-radius: var(--radius-lg);
        padding: 1.5rem;
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.5);
        transform: rotate(-2deg);
        animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
        0%,
        100% {
            transform: translateY(0) rotate(-2deg);
        }
        50% {
            transform: translateY(-20px) rotate(1deg);
        }
    }

    .dots {
        display: flex;
        gap: 0.5rem;
        margin-bottom: 1.5rem;
    }

    .dots span {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: #333;
    }

    pre {
        color: var(--accent);
        font-family: "Fira Code", monospace;
    }

    .features {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 2rem;
        padding: 0 2rem 8rem;
    }

    .feature-card {
        background: var(--bg-secondary);
        border: 1px solid var(--border-primary);
        padding: 2.5rem;
        border-radius: var(--radius-lg);
        transition: transform 0.2s;
    }

    .feature-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent);
    }

    .feature-icon {
        color: var(--accent);
        margin-bottom: 1.5rem;
    }

    .feature-card h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    .feature-card p {
        color: var(--text-secondary);
        line-height: 1.5;
    }

    @media (max-width: 968px) {
        .hero {
            grid-template-columns: 1fr;
            text-align: center;
        }
        .hero-cta {
            justify-content: center;
        }
        .hero-content h1 {
            font-size: 3rem;
        }
        .hero-visual {
            display: none;
        }
        .features {
            grid-template-columns: 1fr;
        }
    }
</style>
